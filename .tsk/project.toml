runtime = "sysbox-runc"
docker_cache = true
layers = ["dind"]

[init]
script = "./start-services.sh"
work_dir = "/workspace"

[env]
NODE_ENV = "development"

[services]
frontend = { port = 4200, path = "/", priority = 1 }
gateway = { port = 8000, path = "/api", strip_prefix = false, priority = 10 }
